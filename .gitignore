# 로컬 어드민 (커밋 제외)
manage/
key.txt
.*.kate-swp
*.kate-swp

# 비밀 환경변수 (.env, .env.local, .env.from.railway 등 모든 변형 차단; example만 허용)
.env
.env.*
**/.env
**/.env.*
*.env
!.env.example
!*.env.example
# 프로덕션 DB URL 등 평문 크리덴셜 파일 (편의용 로컬 파일, 절대 커밋 금지)
env.txt
**/env.txt

# 에이전트·도구 내부 파일
.claude/
.agent/
.omc/
**/.omc/
.superpowers/
.superdesign/tmp/
.playwright-mcp/
.worktrees/
CLAUDE.md

# 디자인 시안·조사 과정 (로컬 전용 스탠드얼론 페이지)
# 경로를 고정한다. 예전엔 `test/` 로 두어 frontend/src/test(vitest 셋업)까지
# 함께 삼켰고, 그 안에 새 파일을 넣으면 조용히 커밋되지 않았다.
/frontend/test/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
*.egg-info/
*.db

# Node
node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# Playwright evaluation screenshots
tmp_screenshot/

# 로컬 docker override (prod DB 연결 등)
docker-compose.override.yml

# 프로덕션 DB 덤프·노선 자료 (로컬 참고용, 절대 커밋 금지)
db_docs/
