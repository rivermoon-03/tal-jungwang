fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
pydantic-settings>=2.3.0
redis[hiredis]>=5.0.0
PyJWT>=2.13.0
bcrypt>=4.0.0
slowapi>=0.1.9
xmltodict>=0.13.0
chardet>=5.2.0
apscheduler>=3.10.0
openpyxl>=3.1.0
prometheus-fastapi-instrumentator>=7.0.0
holidays>=0.50
pywebpush>=2.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
fakeredis>=2.30.0

# 아래는 direct dependency가 아니지만(transitive), OSV 스캔에서 CVE가 잡혀
# 명시적으로 최소 버전을 고정한다. 상위 패키지가 알아서 끌어오는 버전이지만
# 하한선을 걸어 취약 버전으로 해석되지 않도록 강제한다.
aiohttp>=3.14.1     # pywebpush의 transitive dependency (CVE 다수, 최대 심각도 9.1)
idna>=3.15          # httpx/requests/yarl 등의 transitive dependency
mako>=1.3.12        # alembic의 transitive dependency
pygments>=2.20.0    # pytest의 transitive dependency
