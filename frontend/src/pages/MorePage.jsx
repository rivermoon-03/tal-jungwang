/**
 * MorePage — /more 페이지
 * 더보기 탭 + 다크모드/알림 sub-pages (in-component state로 처리)
 */
import MorePageContent from '../components/more/MorePage'

export default function MorePage() {
  return <MorePageContent />
}
